import { Button } from 'antd'

export const Dashboard = () => {
    return (
        <div>
            <h1>Biểu đồ cấp số</h1>
            <Button type='primary'>Click</Button>
        </div>
    );
}